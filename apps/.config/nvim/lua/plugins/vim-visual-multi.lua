return {
  {
    "mg979/vim-visual-multi",
    branch = "master",
    init = function()
      -- Optional: Add any global configuration variables here before the plugin loads
      -- Example: vim.g.VM_maps = {['Find Under'] = '<C-d>'}
    end,
  },
}
