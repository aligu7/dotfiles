screencopy {
    allow_token_by_default = true
    # custom_picker_binary = hyprland-preview-share-picker
    # NOTE: custom picker drops windows whose title contains ', ", $, or `
    # due to a sanitization mismatch between XDPH and the picker's matching logic.
}
